PORT=3001
NODE_ENV=development

# Cookie/session baseline (future-proof, no auth yet)
COOKIE_NAME=beaconry.sid
COOKIE_SAMESITE=lax
COOKIE_SECURE=false

# CORS baseline (for dev when client runs on 5173)
CORS_ORIGIN=http://localhost:5173
CORS_CREDENTIALS=true
